{{#if hasChildren}}
  <li>
    {{#floating-mobile-children-wrapper active=childrenActive toggleChildren=(action "toggleChildren")}}
      {{yield}}
    {{/floating-mobile-children-wrapper}}
  </li>
  <li>
    <button {{action "toggleChildren"}} class="pure-button pure-button-primary floating-button {{if childrenActive "enabled" "disabled"}}" >PLUS</button>
  </li>
{{else}}
  <li>
    {{yield}}
  </li>
{{/if}}
